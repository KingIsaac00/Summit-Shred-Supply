version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands:
        - echo "Building static site"
  artifacts:
    baseDirectory: /
    files:
      - index.html
      - homePage.html
      - homePage.css
      - items.js
      - images/**/*
      - HomePage/**/*
  cache:
    paths: []
